name: Build Vue

on:
  push:
    branches: ["website"]

jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2
    - run: npm install
    - run: npm run build