name: Build Vue

on:
  push:
    branches: ["website"]

jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2
    - id: Build-Vue
      uses: xRealNeon/VuePagesAction@1.0.1
      with:
        username: 'davidaraujo98'
        reponame: '/PECI-05-2022-23/tree/website'
        token: ${{ secrets.GITHUB_TOKEN }}